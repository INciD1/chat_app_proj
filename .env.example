# Copy this file to .env and fill in your own key (get one free at https://developers.giphy.com/)
# Without this set, the GIF picker will show an error but the rest of the chat still works fine.
GIPHY_API_KEY=your_key_here

# Optional overrides
# SECRET_KEY=change-me-in-production
# PORT=5000
